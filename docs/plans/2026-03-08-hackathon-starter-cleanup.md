# Hackathon Starter Cleanup Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove stock Next.js template boilerplate and leave a minimal, demo-ready single-page shell for the BioRender hackathon app.

**Architecture:** Keep everything inside the existing App Router structure. Replace the template landing page with a thin single-page scaffold, simplify layout metadata and CSS, and delete unused static assets so the repo starts from a clean baseline.

**Tech Stack:** Next.js App Router, React, TypeScript, Tailwind CSS v4

---

### Task 1: Replace the default page with a minimal demo shell

**Files:**
- Modify: `app/page.tsx`
- Test: `app/page.tsx` via `npm run lint`

**Step 1: Write the failing test**

There is no existing test runner configured for component tests in this template. For this cleanup task, use static verification by removing the current template-specific content and then verifying the code shape with lint/build.

**Step 2: Run test to verify it fails**

Inspect the current page and confirm it still contains template branding such as `next.svg`, Vercel links, and starter copy.

Expected: template references are present before edits.

**Step 3: Write minimal implementation**

Replace the page with:

- a simple page wrapper
- a heading and short description
- a disabled-looking company URL input area
- empty cards for summary, use case, prompt, live VM, and action log

**Step 4: Run test to verify it passes**

Run: `npm run lint`

Expected: PASS with no lint errors from `app/page.tsx`

**Step 5: Commit**

```bash
git add app/page.tsx
git commit -m "refactor: replace default nextjs landing page"
```

### Task 2: Simplify layout metadata and global styling

**Files:**
- Modify: `app/layout.tsx`
- Modify: `app/globals.css`
- Test: `app/layout.tsx`, `app/globals.css` via `npm run lint`

**Step 1: Write the failing test**

Confirm the layout and CSS still contain template defaults:

- `Create Next App`
- `Generated by create next app`
- Geist font setup
- starter theme variables

**Step 2: Run test to verify it fails**

Inspect the files before editing.

Expected: template defaults are present.

**Step 3: Write minimal implementation**

Update metadata to BioRender demo text, remove unnecessary font boilerplate, and reduce global CSS to a small reset plus neutral app-wide styles.

**Step 4: Run test to verify it passes**

Run: `npm run lint`

Expected: PASS with no lint errors from layout or CSS imports.

**Step 5: Commit**

```bash
git add app/layout.tsx app/globals.css
git commit -m "refactor: simplify starter layout and styles"
```

### Task 3: Remove unused template assets and verify production build

**Files:**
- Delete: `public/file.svg`
- Delete: `public/globe.svg`
- Delete: `public/next.svg`
- Delete: `public/vercel.svg`
- Delete: `public/window.svg`
- Delete: `app/favicon.ico`
- Test: full app via `npm run build`

**Step 1: Write the failing test**

Confirm the files exist and are only template leftovers.

**Step 2: Run test to verify it fails**

Inspect the repo references and verify these assets are still present before deletion.

Expected: files exist and are not needed after page cleanup.

**Step 3: Write minimal implementation**

Delete the unused template assets and favicon.

**Step 4: Run test to verify it passes**

Run: `npm run build`

Expected: PASS with a successful production build and no missing asset references.

**Step 5: Commit**

```bash
git add app public
git commit -m "chore: remove nextjs starter assets"
```
